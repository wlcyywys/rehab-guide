# 患者康复指导网页 · 开发规格说明（SPEC）

> 本文件供后续开发会话（如 Claude Code）直接按此实现。**本仓库目前只有素材与规格，网页尚未开发。**

## 1. 项目目标

为 **肘关节骨关节炎（OA）合并僵硬 · 关节镜下松解术** 术后患者，提供一个手机端康复指导网页。
患者在门诊/出院时 **扫描二维码** 打开，按术后时间找到对应阶段，观看每个动作的循环短视频并阅读要点与注意事项。

- 目标用户：术后患者及家属，**很多为中老年人**，使用微信扫码在手机上打开（需兼容微信内置浏览器、iOS Safari、Android Chrome）。
- 语言：简体中文。
- 发布：GitHub Pages（免费），公开网址：**https://wlcyywys.github.io/rehab-guide/**

## 2. 技术约束（必须遵守）

| 项目 | 要求 |
|---|---|
| 技术栈 | 纯静态：HTML + CSS + 原生 JavaScript，**无构建步骤**（无 npm/webpack/框架），直接双击 `index.html` 或任意静态服务器可运行 |
| 部署 | GitHub Pages，**从 `main` 分支根目录（/ root）发布**（首选）；如需分离，可改为 `/docs`，但需同步修改素材相对路径 |
| 外部依赖 | **不使用任何外部 CDN**、Google Fonts、外部统计/广告/追踪脚本；**不设置 Cookie**、不用 localStorage 记录个人信息（可用于记住字体大小偏好，非必需） |
| 隐私 | 页面及仓库中**不得出现任何患者信息**；不收集任何输入（无表单提交） |
| 路径 | 全部使用**相对路径**（如 `assets/videos/moves/move01.mp4`），保证在 `/rehab-guide/` 子路径下正常工作 |
| 字体 | 使用系统字体栈：`-apple-system, "PingFang SC", "Microsoft YaHei", "Noto Sans CJK SC", sans-serif` |
| 体积 | 首页首屏尽量轻：视频不自动加载全部，使用 `preload="none"` 或 `metadata`，按需/进入视口时加载（IntersectionObserver） |

## 3. 素材清单（已在仓库中）

```
assets/
├── posters/                 # 用户原始康复海报（4张，1024×1536 JPG），内容见第7节
│   ├── stage1.jpg           # 第1阶段：早期活动（动作01–06）
│   ├── stage2.jpg           # 第2阶段：巩固活动度（动作07–12）
│   ├── stage3.jpg           # 第3阶段：恢复力量（动作13–18）
│   └── stage4.jpg           # 第4阶段：回归生活与运动（19–24）
├── videos/
│   ├── moves/move01.mp4 … move24.mp4   # 每个动作一个 5 秒短片，1280×720 H.264，无音轨，已 faststart
│   ├── stages/stage1.mp4 … stage4.mp4  # 每阶段合集（约38.5秒，1920×1080，无音轨）
│   └── elbow-rehab-full.mp4            # 四阶段完整合集（约2分27秒，1920×1080，无音轨）
└── qr/
    ├── qr-pages.svg          # 指向 https://wlcyywys.github.io/rehab-guide/ 的二维码（矢量）
    └── qr-pages.png          # 同上（位图）
content/
└── rehab-data.json           # 全部阶段/动作的结构化文字（由海报转录），网页应以此为唯一数据源
```

说明：
- 动作短片由海报画面制作（画面基本为静态图卡 + 顶部阶段标签 + 底部动作名），**不是真人实拍动态视频**。网页中按“循环短视频”处理即可；日后如替换为真人示范视频，保持同名文件即可无缝替换。
- 视频播放属性统一：`<video muted loop playsinline webkit-playsinline autoplay preload="metadata">`（静音 + playsinline 才能在 iOS/微信内自动播放）；同时提供点击暂停/播放，并给出 `poster` 占位（可选：用 JS 截首帧，或不设置）。
- 如需新视频素材，单文件须 < 50 MB，编码为 H.264（yuv420p, main/high profile）+ `-movflags +faststart`。

## 4. 页面结构

### 4.1 首页 `index.html`
1. **顶部标题**：「肘关节松解术后康复指导」，副标题：「肘关节OA合并僵硬 · 关节镜松解术后」。
2. **医生/医院信息占位**（显眼位置，由用户后续填写）：
   - `【待填写：医生姓名】` · `【待填写：医院/科室名称】` · `【待填写：复诊/咨询方式（可选）】`
   - 实现为 `content/rehab-data.json` 中 `doctor` 字段 + HTML 中带注释的占位，便于用户只改一处。
3. **适用范围提示**：「适用于单纯清理／骨赘切除／关节囊松解且肘关节稳定者；合并修复手术另定方案。」
4. **康复时间轴（Timeline）**：竖向时间轴，4 张大卡片，点击进入对应阶段页：
   | 阶段 | 名称 | 时间 |
   |---|---|---|
   | ① | 早期活动：消肿，保住活动度 | 术后当天至第2周（医生确认后尽早开始） |
   | ② | 巩固活动度：弯曲、伸直、旋转 | 术后第2–6周（继续第一阶段动作） |
   | ③ | 恢复力量：轻阻力，慢慢加 | 术后约第4–12周（约4–6周评估启动） |
   | ④ | 回归生活与运动：按任务放行 | 术后约1周至6个月以上（不同任务不同时间） |
   - 注意阶段时间**有重叠**，时间轴需体现“重叠/衔接”，并注明“到时间不等于自动放行，以医生评估为准”。
   - 可选（加分项）：“我是术后第几周？”快捷选择（第0–2周/2–6周/4–12周/3个月以上）→ 高亮推荐阶段。纯前端，不存储。
5. **危险信号卡片**（醒目红/橙色，首页也要显示，内容取自动作23）：手指发凉苍白、快速加重的肿痛或明显无力 → 立即就医；新发或加重的无名指/小指麻木、发热、伤口红肿渗液 → 尽快联系医生。
6. **完整视频入口**：可播放 `assets/videos/elbow-rehab-full.mp4`（带 controls，不自动播放）。
7. **页脚免责声明**（每个页面都必须有）：**「本页面仅供康复参考，请遵医嘱」**。

### 4.2 阶段页 `stage1.html` … `stage4.html`（或 `stage.html?id=1` 单模板，二选一；推荐 4 个独立 HTML 由同一 JS 渲染，保证无 JS 时也至少显示标题与海报）
1. 顶部：返回首页按钮（大按钮）、阶段编号与名称、时间范围。
2. 阶段说明（intro）与建议起始量。
3. **动作卡片列表**（每阶段 6 个），每张卡片包括：
   - 动作编号 + 名称（大字）
   - 开始时间标签（when，如「通常当天或次日起」）
   - 循环短视频（`assets/videos/moves/moveXX.mp4`，宽度 100%，16:9）
   - 要点（points，列表，逐条大字显示；次数/组数/时间等数字加粗）
   - 动作23 为警示类卡片，用红色样式
4. **本阶段注意事项**（cautions，醒目色块）。
5. 第4阶段额外显示「医生填写」区（doctorForm）：允许活动范围、允许提重（kg）、抗阻开始周、撑压开始周、复诊日期——**仅作为纸面/口头提示的空白展示，不做可提交表单**。
6. “查看原始海报”折叠区：显示对应 `assets/posters/stageN.jpg`，点击可全屏放大（支持双指缩放，直接用新标签打开图片即可）。
7. 可选：“播放本阶段合集”(`assets/videos/stages/stageN.mp4`)。
8. 底部：上一阶段 / 下一阶段 大按钮；免责声明。

### 4.3 二维码页（可选）`qr.html`
- 显示 `assets/qr/qr-pages.svg`（大尺寸，便于打印张贴），下方显示网址与医生/医院占位，适合打印 A4 张贴在诊室。
- 若仓库名或网址变化，需重新生成二维码（可用 Python `segno`：`segno.make(url, error='m').save('assets/qr/qr-pages.svg', scale=10, border=4)`）。

## 5. 视觉与无障碍（面向中老年患者）

- **正文字号 ≥ 18px（建议 20px）**，标题 ≥ 26px，行高 1.6–1.8；按钮最小点击区域 48×48px。
- 提供“**大字/超大字**”切换按钮（A / A+ / A++，通过切换根元素 font-size 实现）。
- 高对比度：深蓝（#0B3A6E 附近，呼应海报）为主色，橙色（#F07A1E 附近）为强调/时间标签，警示用红色；文字与背景对比度满足 WCAG AA。
- 移动优先：以 360–430px 宽为主设计；平板/桌面居中最大宽度约 720px。
- 支持系统深色模式可选，不强制。
- 图片、视频提供 `alt`/`aria-label`（动作名）。
- 尊重 `prefers-reduced-motion`：此时视频不自动播放，显示播放按钮。
- 不使用小于 14px 的任何文字（包括页脚）。

## 6. 文件结构建议（开发后）

```
/ (main 分支根目录，GitHub Pages 源)
├── index.html
├── stage1.html … stage4.html     (或 stage.html)
├── qr.html                        (可选)
├── css/style.css
├── js/app.js                      # 读取 content/rehab-data.json 渲染（fetch 相对路径）
├── content/rehab-data.json
├── assets/...
├── .nojekyll                      # 避免 Jekyll 处理
├── README.md
└── SPEC.md
```
注意：`fetch()` 读取 JSON 在 `file://` 下会失败；可同时提供 `content/rehab-data.js`（`window.REHAB_DATA = {...}`）作为回退，或将数据直接内嵌于 JS。二者内容必须一致（建议以 JSON 为准，写一个说明）。

## 7. 内容转录（来自4张海报，网页文字以 `content/rehab-data.json` 为准）

**标题**：肘关节OA合并僵硬 · 关节镜松解术后康复
**适用**：单纯清理／骨赘切除／关节囊松解且肘关节稳定者；合并修复手术另定方案。

### 第1阶段 早期活动：消肿，保住活动度（术后当天至第2周｜医生确认后尽早开始）
- 一般当天或次日启动；神经阻滞未退时，由医护指导保护患肢。
- 建议起始量：动作02–06每次5–10遍，每日4–6轮；少量多次，按反应调整。

| 编号 | 动作 | 开始时间 | 要点 |
|---|---|---|---|
| 01 | 抬高手臂＋冷敷 | 术后即开始｜感觉恢复后冷敷 | 枕头托住前臂，手略高于心脏，避免压住肘内侧；冷敷隔毛巾10–15分钟；敷料保持干燥 |
| 02 | 握拳张手＋手腕活动 | 术后当天起 | 轻轻握拳再张开五指，手腕缓慢上翘、下弯；前臂有支撑，不用力捏球 |
| 03 | 辅助屈肘：手靠近肩 | 通常当天或次日起 | 上臂靠近身体，健手托住患侧前臂；患肘慢慢弯曲再放回，只到可耐受范围 |
| 04 | 辅助伸肘：慢慢伸直 | 通常当天或次日起 | 上臂和前臂有支撑，健手托着患侧前臂；缓慢伸直再放松，不压肘、不突然掰直 |
| 05 | 旋后：掌心翻向上 | 通常当天或次日起 | 肘贴体侧、尽量屈约90°，从拇指向上开始；前臂转动至掌心朝上，肩膀不跟着转 |
| 06 | 旋前：掌心翻向下 | 通常当天或次日起 | 保持肘的位置，从拇指向上开始；前臂转动至掌心朝下，不甩臂、不扭手腕 |

注意：轻牵拉可以，锐痛、电击感和新发麻木不可以；训练后或次日明显加重，应减量并联系治疗师。夹板、吊带按医嘱使用；避免自行全天固定，也不要自行拆除保护装置。

### 第2阶段 巩固活动度：弯曲、伸直、旋转（术后第2–6周｜继续第一阶段动作）
- 伤口、肿胀及稳定性允许后进阶；不追求每天必须增加几度。
- 以下为建议起始量，治疗师按最受限方向调整；器械并非人人必需。

| 编号 | 动作 | 开始时间 | 要点 |
|---|---|---|---|
| 07 | 屈肘停留：靠近口与肩 | 约第2周起｜获准温和牵伸后 | 健手托前臂慢慢屈肘，上臂不抬高代偿；轻牵拉处停10–20秒，3–5次，每日3–4轮 |
| 08 | 伸肘停留：支撑下伸直 | 约第2周起｜获准温和牵伸后 | 仰卧，上臂垫稳，健手托前臂缓慢伸直；轻牵拉处停10–20秒，3–5次；不悬挂重物 |
| 09 | 前臂旋转：温和到终点 | 第2–6周｜此前已开始主动旋转 | 肘贴身屈约90°，掌心向上、向下交替；各停5–10秒、5次；健手仅轻扶，不强扭 |
| 10 | 肩胛控制：后收再放松 | 第2周起；也可按医嘱更早开始 | 坐直，肩膀放松，肩胛骨轻轻向后靠拢；保持5秒×10次，每日2–3轮；不耸肩 |
| 11 | 牵伸支具：按需定制 | 约第2–6周评估｜可持续数月 | 由治疗师设置屈曲或伸直方向与佩戴时长；低负荷、持续牵伸；麻木、皮肤压痛即停用检查 |
| 12 | CPM：机器辅助屈伸 | 可从术后早期使用｜医生选用 | 角度、速度、每日时长由团队设置，逐步调整；用于辅助康复，仍需主动练习与治疗师指导 |

注意：训练后肿胀不加重、次日活动度不下降，才逐步加量；不要请家属强拉硬掰。伸直不必人人达到0°；以术中安全范围、日常需求和复诊评估制定目标。

### 第3阶段 恢复力量：轻阻力，慢慢加（术后约第4–12周｜约4–6周评估启动）
- 开始条件：伤口愈合、肿胀受控、活动度稳定，且医生允许。
- 建议起始量：每项1组×8–10次，隔日练；耐受后再加组数或阻力。
- 活动度练习仍需每天继续；不要用负重牵拉代替活动练习。

| 编号 | 动作 | 开始时间 | 要点 |
|---|---|---|---|
| 13 | 等长用力：出力不移动 | 约4–6周起｜先轻轻激活 | 肘屈约90°，健手挡住患侧前臂；分别尝试伸肘、弯肘但不移动；轻用力保持5秒，不憋气 |
| 14 | 肱二头肌：轻物弯举 | 约4–6周起｜获准抗阻后 | 肘贴身，掌心朝上，轻物缓慢抬起、放下；先空手，再用少量水的小瓶；不用肩借力 |
| 15 | 肱三头肌：轻阻力伸肘 | 约4–6周起｜获准抗阻后 | 上臂贴身，拉轻弹力带缓慢伸肘，再慢慢回位；固定点须牢靠，不猛拉、不锁死肘关节 |
| 16 | 旋前＋旋后：轻阻力旋转 | 约6周起｜先掌握空手旋转 | 前臂有支撑，肘屈约90°，握短轻棒靠近中点；掌心向上、向下缓慢交替；不拿长柄重锤 |
| 17 | 手腕背伸＋掌屈 | 约4–6周起｜获准抗阻后 | 前臂放桌上，手伸出桌边，握轻小水瓶；背伸：手背靠近前臂；掌屈：手心靠近前臂 |
| 18 | 轻握软球＋放松 | 约4–6周起｜从轻握开始 | 肘与前臂有支撑，轻握软球3秒后完全放松；避免握到发抖或肘痛；不做最大握力挑战 |

注意：只增加一个变量（次数、组数或阻力）；若次日肿胀、疼痛或僵硬明显加重，退回上一级。本页为起始建议，不是统一处方；合并肌腱／韧带修复者须采用专门保护方案。

### 第4阶段 回归生活与运动：按任务放行（术后约1周至6个月以上｜不同任务不同时间）
- 每个阶段均需结合疼痛、肿胀、活动度与力量评估；到时间不等于自动放行。
- 松解术改善卡阻和活动受限，不能让已经磨损的软骨恢复如初。

| 编号 | 项目 | 时间 | 要点 |
|---|---|---|---|
| 19 | 洗漱、吃饭与键盘 | 术后早期至第1–2周｜按耐受逐步恢复 | 先做轻巧短时任务，前臂尽量有支撑；屈肘吃饭、伸手取轻物；避免反复硬撑和搬重物 |
| 20 | 提物与家务 | 约6–8周起｜医生允许后 | 先双手提轻物、贴近身体，再增加患手参与；先加持续时间再加重量；避免突然拧转或猛拽 |
| 21 | 撑墙，再到撑桌 | 约8–12周或更晚｜获准撑压后 | 双手轻撑墙，身体微倾，稳定后再降低支撑高度；每次少量尝试；不直接做俯卧撑或双杠臂屈伸 |
| 22 | 重体力、持拍与投掷 | 通常约3–6个月或更晚 | 先空拍与低强度专项，再渐增次数、速度和负荷；由医生评估放行；高负荷投掷和对抗项目更谨慎 |
| 23 | 出现这些情况，及时就医 | 全程注意｜不要硬练 | 手指发凉苍白、快速加重的肿痛或明显无力：立即就医；新发或加重的无名指/小指麻木，发热、伤口红肿渗液：尽快联系医生 |
| 24 | 记录与复诊：看趋势 | 常见复诊点：约2周、6周、3个月 | 每周记录屈伸与旋转、疼痛和次日肿胀；若活动度持续下降或再次卡锁，提前复诊，勿靠硬掰突破；之后按医嘱随访 |

特殊情况：合并尺神经前移——避免肘内侧受压，按术式调整屈肘与负荷；合并韧带/肌腱修复、骨折固定或软骨修复——活动范围与负重另定，不能套用本时间表。

医生填写（空白展示）：允许活动范围 ____｜允许提重 ____ kg｜抗阻开始 第 ____ 周｜撑压开始 第 ____ 周｜复诊日期 ____；用药按处方；是否预防异位骨化，由医生按风险决定。

## 8. 验收标准

- [ ] 纯静态，无构建步骤；开启 GitHub Pages（main / root）后访问 https://wlcyywys.github.io/rehab-guide/ 正常显示。
- [ ] 手机（iPhone Safari、微信内置浏览器、Android Chrome）上首页 4 阶段时间轴清晰，阶段页 6 个动作视频均可静音循环播放（playsinline，不跳出全屏）。
- [ ] 所有文字与海报/`rehab-data.json` 一致，无错别字；数字（次数、秒数、周数）准确。
- [ ] 正文字号 ≥ 18px，有字号放大按钮；无小于 14px 文字。
- [ ] 每页页脚都有「本页面仅供康复参考，请遵医嘱」。
- [ ] 医生/医院名称为明显的占位符，且只需在一处修改。
- [ ] 无外部 CDN、无统计/追踪代码、无 Cookie、无患者数据；浏览器 Network 面板仅请求本站资源。
- [ ] 二维码扫描后打开的是 Pages 网址。
- [ ] Lighthouse 移动端：Accessibility ≥ 90，Best Practices ≥ 90。
- [ ] 仓库中添加 `.nojekyll`。

## 9. 不在范围内

- 后端、登录、患者数据收集、打卡上传、推送提醒。
- 多病种（本期只做肘关节松解术；数据结构保留 `stages` 数组，将来可扩展其他术式，如新增 `content/<术式>.json`）。
